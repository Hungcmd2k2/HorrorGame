using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class LoadNhuMoi : MonoBehaviour
{
    // Start is called before the first frame update
    public Canvas can;
    void Start()
    {
       can.gameObject.SetActive(false); 
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
